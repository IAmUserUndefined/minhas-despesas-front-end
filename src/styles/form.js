import styled from "styled-components";

const Form = styled.form`
    width: 100%;
    padding: 1rem;
    text-align: center;

    > div {
        margin-bottom: 1.5rem;
    }
`;

export default Form;